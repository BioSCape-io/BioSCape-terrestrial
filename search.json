[
  {
    "objectID": "botanist_photo_processing.html",
    "href": "botanist_photo_processing.html",
    "title": "BioSCape Photo Processing",
    "section": "",
    "text": "Required libraries were loaded and a versioned release tag was generated from the current date to label all output files and GitHub releases produced by this workflow.\nShow the code\n# Load necessary libraries\nlibrary(exifr)       # EXIF metadata extraction (used in eval=F photo-copy chunk)\nlibrary(lubridate)   # date and time parsing\nlibrary(stringr)     # string operations\nlibrary(tidyverse)   # dplyr, purrr, ggplot2, readr, tibble, tidyr\nlibrary(googledrive) # Google Drive download\nlibrary(readxl)      # read Excel workbooks\nlibrary(knitr)       # kable tables\nlibrary(piggyback)   # GitHub release upload/download\nlibrary(leaflet)     # interactive maps\nlibrary(magick)      # HEIC → JPEG conversion (used in eval=F photo-copy chunk)\nlibrary(DT)          # interactive datatable output\nlibrary(sf)          # spatial data and distance calculations\nlibrary(stringdist)  # Jaro-Winkler fuzzy string matching\n\ntagdate=paste0(\"v\",format(today(),\"%Y%m%d\"))\ntag=paste0(\"vegphoto_\",tagdate)\n#tag = \"vegphoto_v20241126\"\nif(F){\npb_new_release(\"BioSCape-io/BioSCape-terrestrial\",tag=tag)\n}"
  },
  {
    "objectID": "botanist_photo_processing.html#download-current-version-of-spreadsheet",
    "href": "botanist_photo_processing.html#download-current-version-of-spreadsheet",
    "title": "BioSCape Photo Processing",
    "section": "Download current version of spreadsheet",
    "text": "Download current version of spreadsheet\nThe master photo metadata spreadsheet (Rarefaction_FA.xlsx) was downloaded from Google Drive. This file contains one worksheet per botanist, each populated from the metadata of photographs taken in the field using a standardised template capturing filename, GPS coordinates, timestamp, species identification, and photo type flags.\n\n\nShow the code\n######### User settings\nif (Sys.getenv(\"USER\") == \"jasper\") {gmail = \"jasper.slingsby@uct.ac.za\"}\nif (Sys.getenv(\"USER\") == \"adamw\") {gmail = \"adamw@buffalo.edu\"}\n\n# Define the URL or key of the Google Sheets\nfname=\"data/Rarefaction_FA.xlsx\"\nsheet_url=\"https://docs.google.com/spreadsheets/d/13kJ7O4PbH6bM-Y89wGf_tbxs-R1THXlb/edit?usp=sharing&ouid=100268570982256677112&rtpof=true&sd=true\"\n\nif(F) drive_download(sheet_url, path = fname, overwrite = TRUE)"
  },
  {
    "objectID": "botanist_photo_processing.html#parse-and-combine-all-botanist-sheets",
    "href": "botanist_photo_processing.html#parse-and-combine-all-botanist-sheets",
    "title": "BioSCape Photo Processing",
    "section": "Parse and combine all botanist sheets",
    "text": "Parse and combine all botanist sheets\nEach botanist worksheet was read and combined into a single dataset. Rows lacking a timestamp were discarded as incomplete entries. The modify_date field (formatted YYYY:MM:DD HH:MM:SS by the camera firmware) was re-formatted to ISO-8601 and parsed into separate datetime, date, and time columns. GPS coordinate fields were coerced to numeric; cells containing non-numeric text from Excel were silently converted to NA. Photos were classified as either \"plot\" (cardinal direction shots, plot overview, or rarefaction sequence markers) or \"species\" (individual taxon records) based on the content of the genus field. Cardinal directions were matched as exact single-character strings to avoid false positives within genus names. Records with missing GPS coordinates (e.g. screenshots) were excluded, and the resulting table was converted to an sf point layer in WGS 84 (EPSG:4326).\n\n\nShow the code\nsheets = excel_sheets(fname)\n# Read sheets and do a little cleaning for irregularites\n\nsheets &lt;- sheets[-1]  # Drop the old Ross spreadsheet (historical; pre-dates unified workflow)\n\n# Read all botanist sheets and combine into one sf object.\n# purrr::map() iterates over sheet names; bind_rows() stacks the results.\nphoto_all &lt;- map(sheets, \\(i)\n  read_xlsx(fname, i) |&gt;\n    # Drop rows with no timestamp — incomplete entries that cannot be placed on\n    # a timeline and would produce unparseable strings downstream.\n    filter(!is.na(modify_date)) |&gt;\n    mutate(\n      # Camera firmware writes dates as \"YYYY:MM:DD HH:MM:SS\"; replace the\n      # date-portion colons with dashes so lubridate can parse the string.\n      datetime    = ymd_hms(str_replace(modify_date,\n                              \"^(\\\\d{4}):(\\\\d{2}):(\\\\d{2})\", \"\\\\1-\\\\2-\\\\3\")),\n      date        = date(datetime),\n      time        = hms(format(datetime, \"%H:%M:%S\")),\n      modify_date = datetime,\n      # GPS fields can arrive as character \"NA\" from Excel; suppress coercion\n      # warnings because NA → NA is the intended behaviour here.\n      gps_latitude       = suppressWarnings(as.numeric(gps_latitude)),\n      gps_longitude      = suppressWarnings(as.numeric(gps_longitude)),\n      gps_altitude       = suppressWarnings(as.numeric(gps_altitude)),\n      gps_position_error = suppressWarnings(as.numeric(gps_position_error)),\n      plot_number        = as.character(plot_number),\n      # Classify plot-level vs species photos based on the genus field.\n      # Cardinal directions (S, N, W, E) are matched as whole words to avoid\n      # false positives inside genus names (e.g. \"Erica\" contains \"E\").\n      # W2/S2/E2/N2 are sub-cardinal directions used by some botanists.\n      photo_type = case_when(\n        grepl(\"^(plot|start|rarefaction|end|stop|centre|center|magnet|cairn|view|datasheet|webbing|alluvial|unidentified)$\",\n              genus, ignore.case = TRUE)             ~ \"plot\",\n        grepl(\"^[SNWE]$|W2|S2|E2|N2|South\", genus)  ~ \"plot\",\n        TRUE                                         ~ \"species\"\n      )\n    ) |&gt;\n    select(-exposure_time) |&gt;   # drop — inconsistent column type across sheets\n    filter(!is.na(gps_longitude) & !is.na(gps_latitude)) |&gt;  # drop screenshots\n    st_as_sf(coords = c(\"gps_longitude\", \"gps_latitude\"), remove = FALSE,\n             crs = 4326)\n) |&gt;\n  bind_rows()"
  },
  {
    "objectID": "botanist_photo_processing.html#preview-raw-photo-data",
    "href": "botanist_photo_processing.html#preview-raw-photo-data",
    "title": "BioSCape Photo Processing",
    "section": "Preview raw photo data",
    "text": "Preview raw photo data\nThe first rows of the combined photo table are displayed here for a quick sanity check of column names, data types, and representative values — before any spatial or taxonomic processing.\n\n\nShow the code\nkable(head(photo_all))\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\nfolder\nfilename\ndescription\nlocation\nplot_number\ngenus\nspecies\nplot_photo\ninat_photo\nrarefaction_photo\nrarefaction_replicate\nrarefaction_duplicate\nNotes\ngps_latitude\ngps_longitude\ngps_altitude\ngps_position_error\nmodify_date\ndate\ntime\noffset_time\nimage_width\nimage_height\ngps_datestamp\nfile_type\nmedia_group_uuid\nfile_base\nbotanist\nid\nfile_url\ndatetime\nphoto_type\nnotes\ngeometry\n\n\n\n\nBioSCape_Admin/VegPlots/Photos/BioSCape1_Ross//Photos from 2023\nIMG_0006.HEIC\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\n-34.08549\n18.42051\n320.18445\n4.750614\n2023-03-27 13:55:32\n2023-03-27\n13H 55M 32S\n=+02:00\n4032\n3024\n2023:03:27 13:55:32\nHEIC\nNA\nIMG_0006\nRoss\nNA\nNA\n2023-03-27 13:55:32\nspecies\nNA\nPOINT (18.42051 -34.08549)\n\n\nBioSCape_Admin/VegPlots/Photos/BioSCape1_Ross//Photos from 2023\nIMG_0014.HEIC\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\n-34.19239\n24.83940\n21.59512\n4.581170\n2023-04-19 14:19:37\n2023-04-19\n14H 19M 37S\n=+02:00\n4032\n3024\n2023:04:19 14:19:37\nHEIC\nNA\nIMG_0014\nRoss\nNA\nNA\n2023-04-19 14:19:37\nspecies\nNA\nPOINT (24.8394 -34.19239)\n\n\nBioSCape_Admin/VegPlots/Photos/BioSCape1_Ross(1)//Takeout 2/Takeout 19/Google Photos/Photos from 2023\nIMG_0015.HEIC\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\n-34.19237\n24.83940\n22.49422\n4.755119\n2023-04-19 14:20:38\n2023-04-19\n14H 20M 38S\n=+02:00\n4032\n3024\n2023:04:19 14:20:38\nHEIC\nNA\nIMG_0015\nRoss\nNA\nNA\n2023-04-19 14:20:38\nspecies\nNA\nPOINT (24.8394 -34.19237)\n\n\nBioSCape_Admin/VegPlots/Photos/BioSCape1_Ross//Photos from 2023\nIMG_0016.HEIC\nRomulea rosea with Stachys aetiopica\nRomulea rosea with Stachys aetiopica\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\nNA\n-34.19236\n24.83940\n22.38942\n4.773801\n2023-04-19 14:20:57\n2023-04-19\n14H 20M 57S\n=+02:00\n4032\n3024\n2023:04:19 14:20:57\nHEIC\nNA\nIMG_0016\nRoss\nNA\nNA\n2023-04-19 14:20:57\nspecies\nNA\nPOINT (24.8394 -34.19236)\n\n\nBioSCape_Admin/VegPlots/Photos/BioSCape1_Ross//Photos from 2023\nIMG_0017.HEIC\nCederberg.171.South.view\nCederberg\n171.0\nSouth\nview\n1\nNA\nNA\nNA\nNA\nNA\n-32.15117\n19.02682\n895.22056\n4.728455\n2023-05-03 09:37:15\n2023-05-03\n9H 37M 15S\n=+02:00\n3024\n4032\n2023:05:03 09:37:15\nHEIC\nNA\nIMG_0017\nRoss\nNA\nNA\n2023-05-03 09:37:15\nplot\nNA\nPOINT (19.02682 -32.15117)\n\n\nBioSCape_Admin/VegPlots/Photos/BioSCape1_Ross//Photos from 2023\nIMG_0018.HEIC\nCederberg.171.South.view.2\nCederberg\n171.0\nSouth\nview.2\n1\nNA\nNA\nNA\nNA\nNA\n-32.15117\n19.02682\n893.64451\n4.726790\n2023-05-03 09:37:40\n2023-05-03\n9H 37M 40S\n=+02:00\n4032\n3024\n2023:05:03 09:37:40\nHEIC\nNA\nIMG_0018\nRoss\nNA\nNA\n2023-05-03 09:37:40\nplot\nNA\nPOINT (19.02682 -32.15117)\n\n\n\n\n\n\nAssign photos to vegetation plots\nBioSCape vegetation plot centroids and homogeneous-patch polygons were loaded from the versioned spatial products file. The Euclidean distance from each photo to every plot centroid was computed in the projected coordinate system of the plot layer. Each photo was assigned to its nearest plot; assignments were nullified for photos located more than 1,000 m from the nearest centroid, as these are unlikely to represent intended plot observations.\n\n\nShow the code\n# download spatial data\nif(F){\n  pb_download(file=\"BioScapeSpatialProducts2024_11_12.zip\",dest = \"data\")\n  unzip(\"data/BioScapeSpatialProducts2024_11_12.zip\",exdir = \"data/spatial\")\n}\n\nplots=read_sf(\"data/spatial/BioSCapeVegCenters2024_11_12.shp\")\nhomogeneous=read_sf(\"data/spatial/BioSCapeVegPolys2024_11_12.shp\")\n\n\n# For each photo, find its nearest plot centroid and compute the distance.\n# st_nearest_feature handles CRS differences internally and is faster than\n# computing the full n_photos × n_plots distance matrix.\nnearest_idx &lt;- st_nearest_feature(photo_all, plots)\n\ndists &lt;- tibble(\n  plot_closest   = plots$BScpPID[nearest_idx],\n  dist_to_plot_m = round(\n    as.numeric(st_distance(photo_all, plots[nearest_idx, ], by_element = TRUE)),\n    0\n  )\n)\n\nphoto_all2 &lt;-  photo_all |&gt;\n     st_transform(st_crs(plots)) |&gt;\n     bind_cols(dists) |&gt;\n  mutate(plot=ifelse(dist_to_plot_m&gt;1000,NA,plot_closest)) # drop linked plots if taken more than 1km from plot centroid.\n\nif(F){\n\n  photo_all2 |&gt;\n  select(photo_all2,plot,plot_closest,dist_to_plot_m)|&gt; View()\n  identical(photo_all2$plot_closest,photo_all2$plot) #confirm output from two methods above is the same\n\n    photo_all2 |&gt;\n#    filter(dist_to_plot_m&lt;50) |&gt;\n    ggplot(aes(x=dist_to_plot_m))+\n    geom_histogram()#binwidth=0.1)\n\n  }\n# Plot histogram of distance to plot\n\nif(F){\n  photo_all2 |&gt;\n    datatable()\n\n}"
  },
  {
    "objectID": "botanist_photo_processing.html#clean-species-field",
    "href": "botanist_photo_processing.html#clean-species-field",
    "title": "BioSCape Photo Processing",
    "section": "Clean species field",
    "text": "Clean species field\nThe species column from the botanist spreadsheets sometimes contains annotation suffixes encoded as dot-separated tokens (e.g. sulcata.start.rarefaction, cf.gladiata.flat.stems.1) rather than a bare species epithet. Two derived columns are created: species_clean holds the bare epithet (NA when the record only reaches genus level — i.e. when the first token is \"cf\", \"sp\", \"na\", or a bare number), and species_notes captures any remaining content after removing known annotation phrases. The cleaned field is used for fuzzy matching; the original species column is preserved unmodified for traceability.\n\n\nShow the code\n# Recognised annotation phrases that appear as dot-joined suffixes in the\n# species field but are not part of the taxon name.\nannotation_pattern &lt;- regex(\n  \"\\\\b(not in cover|start rarefaction|pink flowers|flat stems)\\\\b\",\n  ignore_case = TRUE\n)\n\n# First-token placeholders that indicate the species epithet is unresolved:\n#   cf  – uncertain identification (species epithet follows as a note)\n#   sp  – species unknown within the genus\n#   na  – not identified\n#   &lt;number&gt; – repeat-photo counter used as species token\nunresolved_terms &lt;- c(\"cf\", \"sp\", \"sp.\", \"na\")\n\nphoto_all2 &lt;- photo_all2 |&gt;\n  mutate(\n    # Replace dot word-separators with spaces and collapse extra whitespace, so\n    # \"cf.gladiata.flat.stems.1\" → \"cf gladiata flat stems 1\".\n    species_raw      = str_squish(str_replace_all(coalesce(species, \"\"), \"\\\\.\", \" \")),\n    species_first    = word(species_raw, 1),\n    # Species is resolved to epithet level unless the first token is a placeholder\n    # term, a bare number, or a \"sp\" + number code (e.g. sp1, sp2 — genus-level\n    # identifications where the botanist numbered multiple morphospecies within\n    # the same genus rather than providing a species epithet).\n    species_resolved = species_first != \"\" &\n                       !str_to_lower(species_first) %in% unresolved_terms &\n                       !str_detect(species_first, \"^\\\\d+$\") &\n                       !str_detect(str_to_lower(species_first), \"^sp\\\\d+$\")\n  ) |&gt;\n  mutate(\n    # Bare epithet for resolved identifications; NA for genus-only records.\n    species_clean = if_else(species_resolved, species_first, NA_character_),\n    # Content beyond the epithet (or the whole string for unresolved records),\n    # stripped of annotation phrases and bare repeat-photo counters.\n    species_rest  = if_else(\n      species_resolved,\n      coalesce(str_squish(word(species_raw, 2, -1)), \"\"),\n      species_raw\n    ),\n    species_rest  = str_squish(str_remove_all(species_rest, annotation_pattern)),\n    # Remove leading or trailing bare numbers (repeat-photo counters, e.g. \"1\").\n    species_rest  = str_squish(str_remove(species_rest, \"^\\\\d+\\\\s*|\\\\s*\\\\d+$\")),\n    species_notes = if_else(species_rest == \"\", NA_character_, species_rest)\n  ) |&gt;\n  select(-species_raw, -species_first, -species_resolved, -species_rest)\n\ncat(\n  \"Species photos with cleaned epithet (species_clean not NA): \",\n  sum(!is.na(photo_all2$species_clean[photo_all2$photo_type == \"species\"]),\n      na.rm = TRUE), \"\\n\",\n  \"Species photos genus-only (species_clean is NA):            \",\n  sum( is.na(photo_all2$species_clean[photo_all2$photo_type == \"species\"]) &\n       !is.na(photo_all2$genus[photo_all2$photo_type == \"species\"]),\n      na.rm = TRUE), \"\\n\"\n)\n\n\nSpecies photos with cleaned epithet (species_clean not NA):  4063 \n Species photos genus-only (species_clean is NA):             5243"
  },
  {
    "objectID": "botanist_photo_processing.html#manual-overrides-for-non-species-photo-names",
    "href": "botanist_photo_processing.html#manual-overrides-for-non-species-photo-names",
    "title": "BioSCape Photo Processing",
    "section": "Manual overrides for non-species photo names",
    "text": "Manual overrides for non-species photo names\nSome entries in the botanist spreadsheets use the genus field for free-text notes (landscape descriptions, common names, data-entry labels) that are not taxon names. These slip past the photo_type classifier and would otherwise be passed to the fuzzy matcher, producing spurious corrections. The table below lists known cases; set corrected_name to NA to exclude an entry from species matching entirely, or to a valid \"Genus species\" string to force a specific canonical match. Add new rows here as they are discovered during review. This table is applied to the fuzzy-matching results in the Apply corrections to the photo dataset section below.\n\n\nShow the code\n# photo_name values are the cleaned \"genus [species_clean]\" strings that reach\n# the fuzzy matcher.  NA corrected_name → excluded from species list entirely.\nmanual_overrides &lt;- tribble(\n  ~photo_name,                                        ~corrected_name,\n  \"View of 8500 year old eroded alluvial sediments\",  NA_character_,\n  \"? not\",                                            NA_character_,\n  \"unknown\",                                          NA_character_,\n  \"Vygie not\",                                        NA_character_,\n  \"vygie not\",                                        NA_character_,\n  \"seen from_E_on_6_May\",                             NA_character_,\n  \"datasheet1\",                                       NA_character_,\n  \"datasheet2\",                                       NA_character_,\n  \"red webbing\",                                      NA_character_,\n  \"No idea\",                                          NA_character_,\n  \"Thymelaeaceae probably\",                            NA_character_,\n  \"buck droppings\",                                   NA_character_,\n  \"much dung\",                                        NA_character_,\n  \"Views of\",                                         NA_character_,\n  \"large friendly\",                                   NA_character_,\n)"
  },
  {
    "objectID": "botanist_photo_processing.html#join-with-plot-level-species-lists",
    "href": "botanist_photo_processing.html#join-with-plot-level-species-lists",
    "title": "BioSCape Photo Processing",
    "section": "Join with plot-level species lists",
    "text": "Join with plot-level species lists\nThe vegetation survey species list records species actually observed and identified during structured vegetation surveys at each plot. Survey identifications are treated as the authoritative name reference. Photo-recorded names are corrected against this list using Jaro-Winkler fuzzy string matching before any cross-dataset comparison is performed, so that minor field-recording spelling errors do not inflate mismatch counts.\n\nLoad survey species data\n\n\nShow the code\n# Path to the versioned vegetation survey species file (plot × species rows).\n# Download is wrapped in if(F) so it only runs manually when an updated file is needed.\nspd_file &lt;- \"data/bioscape_veg_plot_species_v20241104.csv\"\nif (F) {\n  download.file(\n    \"https://github.com/BioSCape-io/BioSCape-terrestrial/releases/download/v20241104/bioscape_veg_plot_species_v20241104.csv\",\n    destfile = spd_file\n  )\n}\n\nspd &lt;- read_csv(spd_file, show_col_types = FALSE)\n\n# Per-plot species list from the vegetation survey — the authoritative reference.\n# Genus_Species_combo contains \"Genus species\"; word() splits on the first space.\nspd_species &lt;- spd |&gt;\n  filter(!is.na(Genus_Species_combo)) |&gt;\n  mutate(\n    plot = Plot,\n    genus     = word(Genus_Species_combo, 1),\n    species   = word(Genus_Species_combo, 2),\n    genus_l   = str_to_lower(str_trim(genus)),\n    species_l = str_to_lower(str_trim(species)),\n    spd_name  = paste(genus, species)\n  ) |&gt;\n  distinct(plot, genus, species, genus_l, species_l, spd_name)\n\n# Canonical accepted-species list covering all taxa observed across BioSCape\n# plots (both survey and photo data).  This is the fuzzy-matching target —\n# photo names are corrected to the nearest canonical name.\ncanonical_file   &lt;- \"data/BioSCapePlots_AcceptedSpecies.csv\"\ncanonical_names  &lt;- read_csv(canonical_file, show_col_types = FALSE) |&gt;\n  pull(Taxon)\n\ncat(\"Unique plot × species combinations in survey data:\", nrow(spd_species), \"\\n\")\n\n\nUnique plot × species combinations in survey data: 1974 \n\n\nShow the code\ncat(\"Canonical accepted taxon names (matching target): \", length(canonical_names), \"\\n\")\n\n\nCanonical accepted taxon names (matching target):  11733 \n\n\n\n\nFuzzy-correct photo names against the canonical species list\nMatching is performed in two sequential stages using the Jaro-Winkler string distance metric (p = 0.1 prefix weight). Stage 1 matches the photo genus against all canonical genera; a photo name that fails to find a close genus match is immediately flagged \"no_match\" and receives no correction. Stage 2 matches the species epithet against only the species recorded under the matched genus, constraining the candidate set to one genus. This two-stage design prevents a well-matching species epithet from pulling a poorly-matched genus across a genus boundary (e.g. “myrovernix glandulosa” would otherwise be corrected to “Rosenia glandulosa” because the epithet “glandulosa” is shared). When the genus matches but no species epithet clears its threshold, the entry is classified \"genus_only\" rather than forced into the nearest species. Corrections are made at the name level (not per plot), because a misspelling is a property of the entered string, not of the plot where it was recorded.\n\n\nShow the code\n# Separate Jaro-Winkler thresholds for genus and species matching.\n# The genus threshold is tighter — genus misspellings are typically small\n# (1–2 character transpositions), so a weak genus match almost certainly\n# indicates a non-taxon entry rather than a correctable spelling error.\njw_threshold_genus   &lt;- 0.15   # genus: reject if best canonical genus is &gt; 0.15 away\njw_threshold_species &lt;- 0.20   # species: slightly wider to catch common misspellings\n\n# ── Build canonical reference tables ──────────────────────────────────────────\n# Split canonical names into lowercase genus and species columns for two-stage\n# matching; retain the original mixed-case string for output.\ncanonical_df &lt;- tibble(canonical_name = canonical_names) |&gt;\n  mutate(\n    genus_l   = str_to_lower(word(canonical_name, 1)),\n    species_l = str_to_lower(word(canonical_name, 2))\n  )\n\n# Unique canonical genera — target set for Stage 1 genus matching.\ncanonical_genera &lt;- unique(canonical_df$genus_l)\n\n# ── Distinct photo taxon names ─────────────────────────────────────────────────\n# Use species_clean (dots removed, annotation suffixes stripped, unresolved\n# epithets set to NA) so that noise in the raw species field does not inflate\n# string distances during matching.\nphoto_names_distinct &lt;- photo_all2 |&gt;\n  st_drop_geometry() |&gt;\n  filter(photo_type == \"species\", !is.na(genus), !is.na(plot)) |&gt;\n  mutate(\n    photo_name = if_else(\n      is.na(species_clean),\n      str_trim(genus),\n      paste(str_trim(genus), str_trim(species_clean))\n    )\n  ) |&gt;\n  distinct(photo_name) |&gt;\n  mutate(\n    photo_genus_l   = str_to_lower(word(photo_name, 1)),\n    # Species epithet is NA for single-word (genus-only) entries.\n    photo_species_l = if_else(\n      str_detect(photo_name, \" \"),\n      str_to_lower(word(photo_name, 2)),\n      NA_character_\n    ),\n    is_genus_only = is.na(photo_species_l)\n  )\n\n# ── Stage 1: Genus matching ────────────────────────────────────────────────────\n# Match each photo genus against all canonical genera.  Only names whose best\n# genus match is within jw_threshold_genus proceed to Stage 2; all others are\n# immediately assigned \"no_match\" without any species correction applied.\n# This prevents non-taxon entries like \"resprouting Polygalaceous\" from being\n# matched — they fail at genus level before reaching species comparison at all.\ngenus_dist_matrix &lt;- stringdistmatrix(\n  photo_names_distinct$photo_genus_l,\n  canonical_genera,\n  method = \"jw\", p = 0.1\n)\n\nphoto_names_distinct &lt;- photo_names_distinct |&gt;\n  mutate(\n    matched_genus_l = canonical_genera[apply(genus_dist_matrix, 1, which.min)],\n    genus_dist      = round(apply(genus_dist_matrix, 1, min), 4),\n    genus_matched   = genus_dist &lt; jw_threshold_genus\n  )\n\n# ── Stage 2: Species matching within matched genus ─────────────────────────────\n# For names that passed genus matching and carry a species epithet, match the\n# epithet against only the species recorded under the matched genus in the\n# canonical list.  Constraining the candidate set to one genus prevents a\n# shared or similar epithet from pulling the match across a genus boundary\n# (e.g. \"myrovernix glandulosa\" cannot reach \"Rosenia glandulosa\" because\n# \"myrovernix\" fails genus matching before species comparison is attempted).\n# When the genus matches but the species epithet does not clear its threshold,\n# the entry is classified \"genus_only\" rather than forced into the nearest\n# species — the epithet is treated as unresolved, not misspelled.\nspecies_results &lt;- photo_names_distinct |&gt;\n  filter(genus_matched, !is_genus_only) |&gt;\n  mutate(\n    sp_result = map2(\n      photo_species_l, matched_genus_l,\n      \\(sp_l, gen_l) {\n        # Candidate species epithets for this matched genus (lowercase)\n        candidates &lt;- canonical_df$species_l[canonical_df$genus_l == gen_l]\n        if (length(candidates) == 0 || is.na(sp_l)) {\n          return(tibble(\n            matched_species_l = NA_character_,\n            species_dist      = NA_real_,\n            species_matched   = FALSE\n          ))\n        }\n        dists  &lt;- stringdist(sp_l, candidates, method = \"jw\", p = 0.1)\n        best_i &lt;- which.min(dists)\n        tibble(\n          matched_species_l = candidates[best_i],\n          species_dist      = round(dists[best_i], 4),\n          species_matched   = dists[best_i] &lt; jw_threshold_species\n        )\n      }\n    )\n  ) |&gt;\n  unnest(sp_result) |&gt;\n  select(photo_name, matched_species_l, species_dist, species_matched)\n\n# Attach species results back to the full names table.\nphoto_names_distinct &lt;- photo_names_distinct |&gt;\n  left_join(species_results, by = \"photo_name\")\n\n# ── Assemble corrections table ─────────────────────────────────────────────────\n# Join matched genus_l + species_l back to canonical_df to recover the properly\n# capitalised canonical name.  Rows without a passing species match leave\n# canonical_name as NA and are classified \"genus_only\" below.\nphoto_name_corrections &lt;- photo_names_distinct |&gt;\n  left_join(\n    canonical_df |&gt; select(canonical_name, genus_l, species_l),\n    by = c(\"matched_genus_l\" = \"genus_l\", \"matched_species_l\" = \"species_l\")\n  ) |&gt;\n  mutate(\n    match_type = case_when(\n      # Genus-only entry (no species epithet) that cleared the genus threshold\n      is_genus_only & genus_matched                                            ~ \"genus_only\",\n      # Genus-only entry with no genus match — non-taxon free text\n      is_genus_only & !genus_matched                                           ~ \"no_match\",\n      # Binomial where genus failed to match any canonical genus\n      !genus_matched                                                           ~ \"no_match\",\n      # Binomial where genus matched but species epithet did not clear threshold\n      genus_matched & !coalesce(species_matched, FALSE)                       ~ \"genus_only\",\n      # Both stages passed — distinguish exact from fuzzy\n      genus_matched & coalesce(species_matched, FALSE) &\n        str_to_lower(photo_name) == str_to_lower(canonical_name)             ~ \"exact\",\n      genus_matched & coalesce(species_matched, FALSE)                        ~ \"fuzzy\",\n      TRUE                                                                    ~ \"no_match\"\n    ),\n    canonical_name_matched = if_else(\n      match_type %in% c(\"exact\", \"fuzzy\"),\n      canonical_name,\n      NA_character_\n    ),\n    corrected_name = case_when(\n      match_type %in% c(\"exact\", \"fuzzy\") ~ canonical_name,\n      # Recover properly capitalised genus from the canonical list\n      match_type == \"genus_only\"          ~ str_to_title(matched_genus_l),\n      TRUE                                ~ photo_name\n    ),\n    corrected_genus   = word(corrected_name, 1),\n    corrected_species = word(corrected_name, 2),\n    # Report species dist for full binomial matches, genus dist for genus-only\n    match_dist = case_when(\n      match_type %in% c(\"exact\", \"fuzzy\") ~ species_dist,\n      match_type == \"genus_only\"          ~ genus_dist,\n      TRUE                                ~ NA_real_\n    )\n  ) |&gt;\n  select(photo_name, corrected_name, canonical_name_matched, match_type,\n         match_dist, corrected_genus, corrected_species)\n\n# Apply manual overrides: stamp match_type = \"excluded\" and corrected_name = NA\n# (or the forced name) for any photo_name listed in manual_overrides.\noverrides_formatted &lt;- manual_overrides |&gt;\n  mutate(\n    match_type             = \"excluded\",\n    canonical_name_matched = NA_character_,\n    match_dist             = NA_real_,\n    corrected_name         = corrected_name,\n    corrected_genus        = word(corrected_name, 1),\n    corrected_species      = word(corrected_name, 2)\n  )\n\nphoto_name_corrections &lt;- rows_update(\n  photo_name_corrections,\n  overrides_formatted,\n  by = \"photo_name\",\n  unmatched = \"ignore\"\n)\n\ncat(\"Distinct photo taxon names assessed:    \", nrow(photo_name_corrections), \"\\n\")\n\n\nDistinct photo taxon names assessed:     1224 \n\n\nShow the code\ncat(\"  exact match to canonical name:        \",\n    sum(photo_name_corrections$match_type == \"exact\"), \"\\n\")\n\n\n  exact match to canonical name:         830 \n\n\nShow the code\ncat(\"  fuzzy-corrected (genus &lt;\", jw_threshold_genus,\n    \", species &lt;\", jw_threshold_species, \"):\",\n    sum(photo_name_corrections$match_type == \"fuzzy\"), \"\\n\")\n\n\n  fuzzy-corrected (genus &lt; 0.15 , species &lt; 0.2 ): 108 \n\n\nShow the code\ncat(\"  genus-only (species below threshold or not recorded):\",\n    sum(photo_name_corrections$match_type == \"genus_only\"), \"\\n\")\n\n\n  genus-only (species below threshold or not recorded): 192 \n\n\nShow the code\ncat(\"  manually excluded (non-species notes):\",\n    sum(photo_name_corrections$match_type == \"excluded\"), \"\\n\")\n\n\n  manually excluded (non-species notes): 15 \n\n\nShow the code\ncat(\"  no_match (genus dist &gt;=\", jw_threshold_genus, \"):\",\n    sum(photo_name_corrections$match_type == \"no_match\"), \"\\n\")\n\n\n  no_match (genus dist &gt;= 0.15 ): 79 \n\n\n\n\nReview of fuzzy corrections applied\nThe table below shows every photo name that was automatically corrected, along with the nearest verified survey name and the string distance. Review these before any downstream use — a low distance score does not guarantee a correct taxonomic match.\n\n\nShow the code\nphoto_name_corrections |&gt;\n  filter(match_type == \"fuzzy\") |&gt;\n  arrange(match_dist) |&gt;\n  select(photo_name, corrected_name, canonical_name_matched, match_dist) |&gt;\n  datatable(\n    caption = paste0(\n      \"Photo names auto-corrected by two-stage fuzzy matching (genus JW &lt; \",\n      jw_threshold_genus, \", species JW &lt; \", jw_threshold_species,\n      \"). Review before downstream use.\"\n    ),\n    options = list(pageLength = 20, scrollX = TRUE),\n    rownames = FALSE\n  )\n\n\n\n\n\n\n\n\nApply corrections to the photo dataset\nThe correction lookup is joined back to photo_all2, adding corrected_genus, corrected_species, match_type, and match_dist columns. The corrected names are used in all subsequent joins; the original genus / species columns are preserved unmodified for traceability.\n\n\nShow the code\n# Build a temporary join key in photo_all2 that matches the keys used in\n# photo_names_distinct (i.e. uses species_clean, not the raw species field).\nphoto_all2 &lt;- photo_all2 |&gt;\n  mutate(\n    photo_name = if_else(\n      photo_type == \"species\" & !is.na(genus) & !is.na(plot),\n      if_else(\n        is.na(species_clean),\n        str_trim(genus),\n        paste(str_trim(genus), str_trim(species_clean))\n      ),\n      NA_character_\n    )\n  ) |&gt;\n  left_join(\n    select(photo_name_corrections,\n           photo_name, corrected_genus, corrected_species, match_type, match_dist),\n    by = \"photo_name\"\n  ) |&gt;\n  # Fall back to the original genus/species only for rows that had no join key\n  # (non-species photos).  Do NOT fall back for manually excluded entries —\n  # those have match_type == \"excluded\" and corrected_genus intentionally NA.\n  mutate(\n    corrected_genus   = if_else(\n      is.na(match_type) | match_type != \"excluded\",\n      coalesce(corrected_genus, genus),\n      corrected_genus\n    ),\n    corrected_species = if_else(\n      is.na(match_type) | (match_type != \"excluded\" & match_type != \"genus_only\"),\n      coalesce(corrected_species, species),\n      corrected_species\n    )\n  ) |&gt;\n  select(-photo_name)\n\n# Per-plot species list using corrected names — used for all subsequent joins.\nphoto_species &lt;- photo_all2 |&gt;\n  st_drop_geometry() |&gt;\n  filter(photo_type == \"species\", !is.na(corrected_genus), !is.na(plot)) |&gt;\n  mutate(\n    genus_l   = str_to_lower(str_trim(corrected_genus)),\n    species_l = str_to_lower(str_trim(corrected_species))\n  ) |&gt;\n  distinct(plot,\n           genus   = corrected_genus,\n           species = corrected_species,\n           genus_l, species_l)\n\ncat(\"Unique plot × species combinations in photos (corrected):\", nrow(photo_species), \"\\n\")\n\n\nUnique plot × species combinations in photos (corrected): 2867 \n\n\n\n\nSpecies in survey data with no matched photo\nSurveyed species that have no corresponding photo after fuzzy correction. These represent taxa observed during the vegetation survey that were either not photographed during the rarefaction walk, or whose photo names deviate too far from the verified name to be auto-corrected (distance ≥ threshold).\n\n\nShow the code\nspd_no_photo &lt;- spd_species |&gt;\n  anti_join(\n    photo_species,\n    by = c(\"plot\", \"genus_l\", \"species_l\")\n  ) |&gt;\n  select(plot, genus, species) |&gt;\n  arrange(plot, genus, species)\n\ncat(\"Survey species with no matched photo:\", nrow(spd_no_photo), \"\\n\")\n\n\nSurvey species with no matched photo: 515 \n\n\nShow the code\nspd_no_photo |&gt;\n  group_by(plot) |&gt;\n  summarise(\n    n_unmatched  = n(),\n    species_list = paste(paste(genus, species), collapse = \"; \"),\n    .groups = \"drop\"\n  ) |&gt;\n  datatable(\n    caption = \"Surveyed species with no matched botanist photo (by plot, after fuzzy correction)\",\n    options = list(pageLength = 15, scrollX = TRUE),\n    rownames = FALSE\n  )\n\n\n\n\n\n\n\n\nPhotos of species with no survey record\nPhotographed species (after fuzzy correction) that have no corresponding survey record at the same plot. These may represent genuinely new detections, photos taken outside the plot boundary, or names that still deviate too far from any verified survey name to be auto-corrected.\n\n\nShow the code\nphoto_no_spd &lt;- photo_species |&gt;\n  anti_join(\n    spd_species,\n    by = c(\"plot\", \"genus_l\", \"species_l\")\n  ) |&gt;\n  select(plot, genus, species) |&gt;\n  arrange(plot, genus, species)\n\ncat(\"Photo species with no matched survey record:\", nrow(photo_no_spd), \"\\n\")\n\n\nPhoto species with no matched survey record: 1408 \n\n\nShow the code\nphoto_no_spd |&gt;\n  group_by(plot) |&gt;\n  summarise(\n    n_unmatched  = n(),\n    species_list = paste(paste(genus, species), collapse = \"; \"),\n    .groups = \"drop\"\n  ) |&gt;\n  datatable(\n    caption = \"Photographed species with no matching survey record (by plot, after fuzzy correction)\",\n    options = list(pageLength = 15, scrollX = TRUE),\n    rownames = FALSE\n  )"
  },
  {
    "objectID": "botanist_photo_processing.html#plot-level-summary",
    "href": "botanist_photo_processing.html#plot-level-summary",
    "title": "BioSCape Photo Processing",
    "section": "plot-level summary",
    "text": "plot-level summary\nPer-plot summary statistics were computed, including counts of photos by type (species, rarefaction, plot overview, iNaturalist-flagged), the number of unique taxa photographed, rarefaction effort, survey dates, and GPS quality indicators. The summary table was written to disk for use in downstream analyses.\n\n\nShow the code\nplot_summary &lt;- photo_all2 |&gt;\n  st_drop_geometry() |&gt;\n  # Exclude photos with no matched plot before summarising\n  filter(!is.na(plot)) |&gt;\n  group_by(plot) |&gt;\n  summarise(\n    # Botanists who contributed photos to this plot\n    botanist = paste(sort(unique(botanist)), collapse = \", \"),\n\n    # Total photo counts\n    photo_total        = n(),\n    photo_species      = sum(photo_type == \"species\", na.rm = TRUE),\n    photo_rarefaction  = sum(rarefaction_photo == 1, na.rm = TRUE),\n    photo_plot         = sum(photo_type == \"plot\", na.rm = TRUE),\n    # Photos flagged for iNaturalist upload\n    photo_inat         = sum(inat_photo == TRUE, na.rm = TRUE),\n    # Rarefaction duplicates (same individual re-photographed in sequence)\n    photo_rarefaction_duplicate = sum(\n      rarefaction_duplicate == TRUE, na.rm = TRUE\n    ),\n\n    # Species richness: distinct fuzzy-corrected name combinations from\n    # species-type photos; corrected_genus is NA for manually excluded entries,\n    # so the !is.na() guard also drops those.\n    n_species = n_distinct(paste(\n      corrected_genus[photo_type == \"species\" & !is.na(corrected_genus)],\n      corrected_species[photo_type == \"species\" & !is.na(corrected_genus)]\n    )),\n\n    # Count of unique (non-duplicate) rarefaction photos — rarefaction_photo == 1\n    # flags a rarefaction photo; rarefaction_duplicate == TRUE flags re-photographs\n    # of the same individual that should not be double-counted.\n    n_rarefaction_photos = sum(\n      rarefaction_photo == 1 & !coalesce(rarefaction_duplicate, FALSE),\n      na.rm = TRUE\n    ),\n\n    # Survey effort: number of unique dates photos were taken\n    n_dates    = n_distinct(date),\n    date_first = min(date, na.rm = TRUE),\n    date_last  = max(date, na.rm = TRUE),\n\n    # Spatial QC: median distance from photos to the official plot centroid (m)\n    median_dist_to_plot_m = median(dist_to_plot_m, na.rm = TRUE),\n    # GPS precision reported by the device\n    mean_gps_error_m = round(mean(gps_position_error, na.rm = TRUE), 1),\n    .groups = \"drop\"\n  ) |&gt;\n  # Replace -Inf / Inf that arise from max/min on all-NA groups\n  mutate(\n    across(where(is.numeric), \\(x) ifelse(is.infinite(x), NA_real_, x))\n  )\n\n# Save to disk for use in downstream analyses\nplot_summary_file &lt;- paste0(\"data/plot_summary_\", tagdate, \".csv\")\nwrite_csv(plot_summary, plot_summary_file)\n\nplot_summary |&gt;\n  datatable(\n    caption = \"Plot-level photo summary\",\n    options = list(pageLength = 15, scrollX = TRUE),\n    rownames = FALSE\n  )"
  },
  {
    "objectID": "botanist_photo_processing.html#global-photo-diagnostics",
    "href": "botanist_photo_processing.html#global-photo-diagnostics",
    "title": "BioSCape Photo Processing",
    "section": "Global photo diagnostics",
    "text": "Global photo diagnostics\nDataset-level summary statistics were compiled to document overall data completeness, coverage, and quality. Key metrics include total photo count, number of matched plots, botanist contributions, temporal range, and mean per-plot photo density.\n\n\nShow the code\n# Per-plot photo counts (needed for mean ± SD)\nper_plot_counts &lt;- photo_all2 |&gt;\n  st_drop_geometry() |&gt;\n  filter(!is.na(plot)) |&gt;\n  count(plot, name = \"n_photos\")\n\nper_plot_species_counts &lt;- photo_all2 |&gt;\n  st_drop_geometry() |&gt;\n  filter(!is.na(plot), photo_type == \"species\") |&gt;\n  count(plot, name = \"n_species_photos\")\n\nphoto_diagnostics &lt;- tibble(\n  Metric = c(\n    \"Total photos (all sheets)\",\n    \"Total matched plots\",\n    \"Photos unmatched to any plot (&gt; 1 km from nearest centroid)\",\n    \"Distinct botanists\",\n    \"Overall date range\",\n    \"Photos missing coordinates (dropped pre-analysis)\",\n    \"Rarefaction duplicate photos\",\n    \"Mean photos per plot (± SD)\",\n    \"Mean species photos per plot (± SD)\",\n    \"Total unique species across all plots (corrected names)\"\n  ),\n  Value = c(\n    # Total rows in the dataset\n    nrow(st_drop_geometry(photo_all2)),\n\n    # Number of distinct plots with at least one matched photo\n    n_distinct(\n      photo_all2$plot[!is.na(photo_all2$plot)]\n    ),\n\n    # Photos where plot is NA because no centroid was within 1 km (see data load)\n    sum(is.na(photo_all2$plot)),\n\n    # Distinct botanists\n    n_distinct(photo_all2$botanist, na.rm = TRUE),\n\n    # Date range as a string\n    paste(\n      min(photo_all2$date, na.rm = TRUE),\n      \"to\",\n      max(photo_all2$date, na.rm = TRUE)\n    ),\n\n    # Note: photos without GPS were filtered out during loading; count is\n    # therefore zero post-filter but informative to document.\n    \"Filtered during data load (GPS required)\",\n\n    # Rarefaction duplicates flagged in the spreadsheet\n    sum(photo_all2$rarefaction_duplicate == TRUE, na.rm = TRUE),\n\n    # Mean ± SD photos per matched plot\n    paste0(\n      round(mean(per_plot_counts$n_photos), 1),\n      \" ± \",\n      round(sd(per_plot_counts$n_photos), 1)\n    ),\n\n    # Mean ± SD species-type photos per matched plot\n    paste0(\n      round(mean(per_plot_species_counts$n_species_photos), 1),\n      \" ± \",\n      round(sd(per_plot_species_counts$n_species_photos), 1)\n    ),\n\n    # Total unique corrected species names across all plots, species photos only\n    n_distinct(paste(\n      photo_all2$corrected_genus[photo_all2$photo_type == \"species\" &\n                                   !is.na(photo_all2$corrected_genus)],\n      photo_all2$corrected_species[photo_all2$photo_type == \"species\" &\n                                     !is.na(photo_all2$corrected_genus)]\n    ))\n  )\n)\n\nkable(photo_diagnostics, col.names = c(\"Metric\", \"Value\"),\n      caption = \"Global photo diagnostics\")\n\n\n\nGlobal photo diagnostics\n\n\n\n\n\n\nMetric\nValue\n\n\n\n\nTotal photos (all sheets)\n11592\n\n\nTotal matched plots\n189\n\n\nPhotos unmatched to any plot (&gt; 1 km from nearest centroid)\n314\n\n\nDistinct botanists\n4\n\n\nOverall date range\n2023-03-27 to 2023-11-01\n\n\nPhotos missing coordinates (dropped pre-analysis)\nFiltered during data load (GPS required)\n\n\nRarefaction duplicate photos\n1035\n\n\nMean photos per plot (± SD)\n59.7 ± 34.5\n\n\nMean species photos per plot (± SD)\n49.8 ± 32.6\n\n\nTotal unique species across all plots (corrected names)\n992"
  },
  {
    "objectID": "botanist_photo_processing.html#upload-photo-dataset-to-github-release",
    "href": "botanist_photo_processing.html#upload-photo-dataset-to-github-release",
    "title": "BioSCape Photo Processing",
    "section": "Upload photo dataset to GitHub release",
    "text": "Upload photo dataset to GitHub release\nThe processed photo metadata table was written to a versioned CSV file. The sf geometry column was dropped prior to writing, as GPS coordinates are retained as explicit gps_latitude and gps_longitude columns. The file was optionally uploaded to the corresponding GitHub release for archival and downstream use.\n\n\nShow the code\nphoto_all_file=paste0(\"data/vegphoto_all_\",tagdate,\".csv\")\n# Drop sf geometry before writing — the geometry column serialises as WKT and is\n# not needed downstream (lat/lon columns are retained from the original data).\nst_drop_geometry(photo_all2) |&gt;\n  write_csv(photo_all_file)\n\n# upload to github release is manual - run the pb_upload command if desired\nif (F) pb_upload(photo_all_file,tag=tag)"
  },
  {
    "objectID": "botanist_photo_processing.html#rename-and-organize-all-photos",
    "href": "botanist_photo_processing.html#rename-and-organize-all-photos",
    "title": "BioSCape Photo Processing",
    "section": "Rename and organize all photos",
    "text": "Rename and organize all photos\nPhotos were copied from the shared Google Drive source folders into a standardised local directory structure (data/photo_out/&lt;BScpPID&gt;/), where unmatched photos (no GPS-derived plot assignment) are placed in a 00/ subfolder. Each file was renamed according to the convention &lt;BScpPID&gt;_&lt;photo_type&gt;_&lt;YYYYMMDD_HHMMSS&gt;_&lt;botanist&gt;.jpeg, where the botanist suffix disambiguates the rare case of two photographers capturing a photo at the same plot within the same second. Files are copied byte-for-byte with file.copy() to preserve all original EXIF metadata intact; exiftool is then used to add structured provenance fields on top without re-encoding the image. This chunk is set to eval=F and must be run manually after verifying the Google Drive paths.\n\n\nShow the code\nphoto_all3 &lt;- photo_all2 |&gt;\n  st_drop_geometry() |&gt;\n  mutate(\n    # Use \"00\" as the plot folder for photos that could not be matched to a plot.\n    plot_folder = if_else(is.na(BScpPID), \"00\", BScpPID),\n    # Sanitise botanist name for use in a filename (replace spaces / special chars\n    # with underscores).\n    botanist_safe = str_replace_all(botanist, \"[^A-Za-z0-9]\", \"_\"),\n    # Naming convention: &lt;plot&gt;_&lt;type&gt;_&lt;YYYYMMDD_HHMMSS&gt;_&lt;botanist&gt;.jpeg\n    # Appending botanist prevents silent overwrites when two photographers take a\n    # photo at the same plot within the same second.\n    newfile = paste0(\n      plot_folder, \"/\",\n      plot_folder, \"_\", photo_type, \"_\",\n      format(datetime, \"%Y%m%d_%H%M%S\"), \"_\",\n      botanist_safe, \".jpeg\"\n    )\n  )\n\noutput_folder &lt;- \"data/photo_out/\"\n\npurrr::walk(seq_len(nrow(photo_all3)), function(i) {\n\n  input_file  &lt;- file.path(\n    \"data/photos/Photos\",\n    sub(\"BioSCape_Admin/VegPlots/Photos/\", \"\", photo_all3$folder[i]),\n    photo_all3$filename[i]\n  )\n  output_file &lt;- file.path(output_folder, photo_all3$newfile[i])\n\n  if (!dir.exists(dirname(output_file)))\n    dir.create(dirname(output_file), recursive = TRUE)\n\n  input_ext &lt;- str_to_lower(tools::file_ext(input_file))\n\n  if (input_ext %in% c(\"heic\", \"heif\")) {\n    # HEIC/HEIF must be converted to JPEG — re-encoding is unavoidable.\n    # After writing the pixel data, exiftool copies all original metadata\n    # (GPS, camera model, exposure, etc.) from the source HEIC back into the\n    # new JPEG so that provenance is preserved as completely as possible.\n    image_read(input_file) |&gt;\n      image_write(output_file, format = \"jpeg\", quality = 95)\n    # Copy original EXIF/XMP/IPTC from the HEIC source into the new JPEG.\n    # The source path is wrapped in shQuote() because exiftool_call does not\n    # shell-quote elements of the args list (only fnames is quoted), and paths\n    # with spaces would otherwise be split by the shell into multiple tokens.\n    exiftool_call(\n      args = list(\n        \"-overwrite_original\",\n        paste0(\"-TagsFromFile=\", shQuote(input_file)),\n        \"-all:all\"\n      ),\n      fnames = output_file\n    )\n  } else {\n    # For native JPEGs: copy byte-for-byte so ALL original EXIF/IPTC/XMP\n    # metadata (GPS, camera model, exposure settings, colour profile, etc.)\n    # is preserved without any re-encoding loss.\n    file.copy(input_file, output_file, overwrite = TRUE)\n  }\n\n  # Build the corrected binomial for the Subject tag; fall back to the original\n  # genus/species for plot-type photos where corrected_genus is not meaningful.\n  taxon_subject &lt;- if_else(\n    photo_all3$photo_type[i] == \"species\" &\n      !is.na(photo_all3$corrected_genus[i]),\n    paste(photo_all3$corrected_genus[i], photo_all3$corrected_species[i]),\n    \"\"\n  )\n\n  # Use the matched plot ID for the title, falling back to \"unmatched\" for the\n  # \"00\" folder photos so the Title tag is still human-readable.\n  plot_label &lt;- if_else(\n    is.na(photo_all3$BScpPID[i]), \"unmatched\", photo_all3$BScpPID[i]\n  )\n\n  # Sanitise free-text fields that may contain characters that would break the\n  # exiftool command string (double-quotes, backslashes, newlines).\n  sanitise &lt;- function(x) str_replace_all(x, '[\"\\\\\\\\\\n\\r]', \" \")\n\n  # Each field is written as a separate element so that field values containing\n  # spaces do not need additional quoting (exiftool treats each list element as\n  # one argument).\n  cmd &lt;- list(\n    \"-overwrite_original\",\n\n    # XMP / IPTC provenance fields — structured, machine-readable ----------------\n    # Original source path so the renamed copy is always traceable.\n    paste0(\"-XMP:Source=\",\n           sanitise(paste0(photo_all3$folder[i], \"/\", photo_all3$filename[i]))),\n    # Plot identifier as a dedicated XMP field for easy downstream filtering.\n    paste0(\"-XMP:PlotID=\", plot_label),\n    # Botanist name as the EXIF Artist field (standard) and separately in XMP.\n    paste0(\"-EXIF:Artist=\",        sanitise(photo_all3$botanist[i])),\n    paste0(\"-XMP:Creator=\",        sanitise(photo_all3$botanist[i])),\n    # Free-text description recorded in the field spreadsheet.\n    paste0(\"-EXIF:ImageDescription=\",\n           sanitise(photo_all3$description[i])),\n    # Species identification — use corrected name for Subject/Taxon tags.\n    paste0(\"-IPTC:ObjectName=\",    sanitise(taxon_subject)),\n    paste0(\"-XMP:Subject=\",        sanitise(taxon_subject)),\n    # Photo type (species / plot) for easy filtering.\n    paste0(\"-XMP:Type=\",           photo_all3$photo_type[i]),\n    # Title with plot and photo-type context.\n    paste0(\"-EXIF:XPTitle=\",\n           sanitise(paste0(\"BioSCape \", photo_all3$photo_type[i],\n                           \" photo from plot \", plot_label))),\n    # GPS coordinates from the spreadsheet as a cross-check against device GPS.\n    paste0(\"-XMP:GPSLatitude=\",    photo_all3$gps_latitude[i]),\n    paste0(\"-XMP:GPSLongitude=\",   photo_all3$gps_longitude[i]),\n    # Copyright statement for the project.\n    \"-EXIF:Copyright=BioSCape project\",\n    \"-XMP:Rights=BioSCape project\",\n    # Keywords for general discovery.\n    paste0(\"-IPTC:Keywords=BioSCape\"),\n    paste0(\"-IPTC:Keywords=\", sanitise(photo_all3$botanist[i])),\n    paste0(\"-IPTC:Keywords=\", plot_label)\n  )\n\n  exiftool_call(args = cmd, fnames = output_file)\n})"
  },
  {
    "objectID": "rarefaction_plan.html",
    "href": "rarefaction_plan.html",
    "title": "Rarefaction Plan",
    "section": "",
    "text": "Fix bugs and improve robustness in Vegplot_Rarefaction.qmd, which processes the cleaned photo dataset to derive per-plot species accumulation curves and rarefaction estimates.\n\n\n\n\n\n\nProblem: attributeList &lt;- EML::set_attributes(...) inside the dataTable1 list uses &lt;- (assignment to global env) instead of = (list element). The attribute list is silently lost from dataTable1.\nFix: Change attributeList &lt;- to attributeList =.\n\n\n\nProblem: The comparison plot at line 628 uses n_species from vegphoto_rarefied_plot, but the select() on line 280–282 does not include n_species. This will produce an error or silently produce NAs.\nFix: Add n_species (and optionally n_genus) to the select() call that builds vegphoto_rarefied_plot.\n\n\n\nProblem: count = row_number() - 1 assumes photos are time-ordered within each BScpPID group. No arrange(datetime) is called before this, so counts may be assigned in the wrong order if the data arrives unsorted.\nFix: Add arrange(datetime) (within the group) before mutate(count = row_number() - 1).\n\n\n\nProblem: predicted_data %&gt;% filter(botanist == \"\") is called and the result is discarded. This is leftover debugging code that adds visual noise and a stray pipe.\nFix: Remove lines 577–578.\n\n\n\n\n\n\n\nProblem: If rarefaction_replicate is all-NA for a plot, max() returns -Inf with a warning.\nFix: Add na.rm = TRUE; wrap in coalesce(..., 1L) to default to 1 when missing.\n\n\n\nProblem: vegphoto_all_v20250228 is hardcoded. Every time the upstream data is regenerated, this must be manually updated, which is easy to forget.\nFix: Move the version string to a parameter at the top of the script:\nphoto_version &lt;- \"vegphoto_v20250228\"\nphoto_all_file &lt;- paste0(\"data/vegphoto_all_\", photo_version, \".csv\")\n\n\n\nProblem: grepl(\"[0-9]\", species) flags any species name containing a digit as a “replicate”. This is intentional (replicates are named e.g. “sp1”, “sp2”) but is not explained, and could accidentally flag legitimate names.\nFix: Add a comment explaining the convention, e.g.:\n# Replicate photos are flagged with a number in the species field (e.g. \"sp1\", \"sp2\")\n# by the botanists to indicate a repeat observation of the same species.\n\n\n\n\n\n\n\nReplace all %&gt;% with |&gt; throughout the script.\n\n\n\nrdatas is an sf object (inherits geometry from rdata). When used in left_join and ggplot calls, the geometry column may cause unexpected behaviour. Add st_set_geometry(NULL) or st_drop_geometry() when building rdatas.\n\n\n\n\n\n\n\nThe task description notes that after iNat upload, the iNat observation IDs should be joined back to bioscape_veg_plot_species_v20241104.csv. A section should be added to Vegplot_Rarefaction.qmd (or a separate script) to: 1. Download the iNat observation export for the BioSCape project. 2. Join on taxon_name + BScpPID (via location field). 3. Write an updated version of the veg species file with the inat_id column populated."
  },
  {
    "objectID": "rarefaction_plan.html#goal",
    "href": "rarefaction_plan.html#goal",
    "title": "Rarefaction Plan",
    "section": "",
    "text": "Fix bugs and improve robustness in Vegplot_Rarefaction.qmd, which processes the cleaned photo dataset to derive per-plot species accumulation curves and rarefaction estimates."
  },
  {
    "objectID": "rarefaction_plan.html#bug-fixes-critical",
    "href": "rarefaction_plan.html#bug-fixes-critical",
    "title": "Rarefaction Plan",
    "section": "",
    "text": "Problem: attributeList &lt;- EML::set_attributes(...) inside the dataTable1 list uses &lt;- (assignment to global env) instead of = (list element). The attribute list is silently lost from dataTable1.\nFix: Change attributeList &lt;- to attributeList =.\n\n\n\nProblem: The comparison plot at line 628 uses n_species from vegphoto_rarefied_plot, but the select() on line 280–282 does not include n_species. This will produce an error or silently produce NAs.\nFix: Add n_species (and optionally n_genus) to the select() call that builds vegphoto_rarefied_plot.\n\n\n\nProblem: count = row_number() - 1 assumes photos are time-ordered within each BScpPID group. No arrange(datetime) is called before this, so counts may be assigned in the wrong order if the data arrives unsorted.\nFix: Add arrange(datetime) (within the group) before mutate(count = row_number() - 1).\n\n\n\nProblem: predicted_data %&gt;% filter(botanist == \"\") is called and the result is discarded. This is leftover debugging code that adds visual noise and a stray pipe.\nFix: Remove lines 577–578."
  },
  {
    "objectID": "rarefaction_plan.html#robustness-improvements",
    "href": "rarefaction_plan.html#robustness-improvements",
    "title": "Rarefaction Plan",
    "section": "",
    "text": "Problem: If rarefaction_replicate is all-NA for a plot, max() returns -Inf with a warning.\nFix: Add na.rm = TRUE; wrap in coalesce(..., 1L) to default to 1 when missing.\n\n\n\nProblem: vegphoto_all_v20250228 is hardcoded. Every time the upstream data is regenerated, this must be manually updated, which is easy to forget.\nFix: Move the version string to a parameter at the top of the script:\nphoto_version &lt;- \"vegphoto_v20250228\"\nphoto_all_file &lt;- paste0(\"data/vegphoto_all_\", photo_version, \".csv\")\n\n\n\nProblem: grepl(\"[0-9]\", species) flags any species name containing a digit as a “replicate”. This is intentional (replicates are named e.g. “sp1”, “sp2”) but is not explained, and could accidentally flag legitimate names.\nFix: Add a comment explaining the convention, e.g.:\n# Replicate photos are flagged with a number in the species field (e.g. \"sp1\", \"sp2\")\n# by the botanists to indicate a repeat observation of the same species."
  },
  {
    "objectID": "rarefaction_plan.html#code-quality",
    "href": "rarefaction_plan.html#code-quality",
    "title": "Rarefaction Plan",
    "section": "",
    "text": "Replace all %&gt;% with |&gt; throughout the script.\n\n\n\nrdatas is an sf object (inherits geometry from rdata). When used in left_join and ggplot calls, the geometry column may cause unexpected behaviour. Add st_set_geometry(NULL) or st_drop_geometry() when building rdatas."
  },
  {
    "objectID": "rarefaction_plan.html#output-completeness",
    "href": "rarefaction_plan.html#output-completeness",
    "title": "Rarefaction Plan",
    "section": "",
    "text": "The task description notes that after iNat upload, the iNat observation IDs should be joined back to bioscape_veg_plot_species_v20241104.csv. A section should be added to Vegplot_Rarefaction.qmd (or a separate script) to: 1. Download the iNat observation export for the BioSCape project. 2. Join on taxon_name + BScpPID (via location field). 3. Write an updated version of the veg species file with the inat_id column populated."
  },
  {
    "objectID": "inat_plan.html",
    "href": "inat_plan.html",
    "title": "iNat Upload Plan",
    "section": "",
    "text": "Convert the cleaned photo dataset output from botanist_photo_processing.qmd into batch CSV files ready for the iNat bulk uploader tool (workflow/dist/iNat_bulk_uploader.exe).\n\n\n\n\ndata/vegphoto_all_&lt;tagdate&gt;.csv — the versioned output of botanist_photo_processing.qmd, one row per photo, with columns including photo_type, BScpPID, genus, species, date, gps_latitude, gps_longitude, gps_position_error, description, botanist, and newfile (the renamed/organised photo path from the photo renaming step).\n\n\n\n\n\ndata/inat_batches/batch_01.csv, batch_02.csv, … — one CSV per 50 observations, each matching the iNat bulk uploader template exactly.\nThe photo files referenced by media_name_* columns are the renamed files produced by the photo reorganisation step in botanist_photo_processing.qmd (photo_all3$newfile).\n\n\n\n\n\n\n\n\n\n\n\niNat field\nSource column / value\n\n\n\n\nid\nsequential integer within the full table (1, 2, 3, …)\n\n\ntaxon_name\npaste(genus, species)\n\n\ndate_obs\nformat(date, \"%d/%m/%Y\")\n\n\ntime_zone\n\"Africa/Johannesburg\" (fixed)\n\n\ndescription\ncollapsed description values for all photos of that species at that plot\n\n\ntag_list\n\"BioSCape\" (fixed)\n\n\nlatitude\nmean GPS latitude of all photos for that species × plot\n\n\nlongitude\nmean GPS longitude\n\n\nlocation\nBScpPID\n\n\npos_acc\nmax gps_position_error for that species × plot\n\n\ngeoprivacy\n\"open\" (fixed)\n\n\nfield_id1\n(empty)\n\n\nfield_value1\n(empty)\n\n\nmedia_name_1–media_name_10\nbasename(newfile) for each photo of that species at that plot (up to 10)\n\n\n\n\n\n\n\nLoad data — read the versioned photo_all_file CSV from botanist_photo_processing.qmd.\nFilter — keep only photo_type == \"species\" rows with non-NA genus + species + BScpPID.\nGroup — group by BScpPID + taxon_name (one row per species per plot).\nCollapse photos — within each group, take up to 10 photos and spread their basenames into media_name_1 … media_name_10. Record n_photos for reference.\nAssign batch — add a batch column: ceiling(row_number() / 50).\nWrite output — for each unique batch value, write a CSV named data/inat_batches/batch_&lt;sprintf(\"%02d\", batch)&gt;.csv containing only the iNat template columns (no batch column in the output files).\n\n\n\n\n\nphoto_type regex — the current pattern \"plot|S|N|W|E|start|rarefaction|end|stop\" matches single letters S/N/W/E anywhere inside a genus name (e.g. “Erica” → matches “E”). Fix: use \\\\b(plot|start|rarefaction|end|stop)\\\\b|^[SNWE]$ so single-letter cardinal directions only match when the entire genus field is that letter.\nst_drop_geometry() before write_csv — the sf geometry column is currently written into the CSV as WKT, which is not needed downstream. Drop it before writing.\n\n\n\n\n\nThe renamed photo files (newfile) are only generated when the photo renaming chunk in botanist_photo_processing.qmd has been run (eval=F currently — must be run manually). The iNat_prepare.qmd will validate that newfile is populated and warn if not.\niNat batch size = 50 observations per the user’s request.\nUp to 10 media files per observation (the template supports up to 25, but 10 is a practical limit given how many photos per species exist in the dataset)."
  },
  {
    "objectID": "inat_plan.html#goal",
    "href": "inat_plan.html#goal",
    "title": "iNat Upload Plan",
    "section": "",
    "text": "Convert the cleaned photo dataset output from botanist_photo_processing.qmd into batch CSV files ready for the iNat bulk uploader tool (workflow/dist/iNat_bulk_uploader.exe)."
  },
  {
    "objectID": "inat_plan.html#inputs",
    "href": "inat_plan.html#inputs",
    "title": "iNat Upload Plan",
    "section": "",
    "text": "data/vegphoto_all_&lt;tagdate&gt;.csv — the versioned output of botanist_photo_processing.qmd, one row per photo, with columns including photo_type, BScpPID, genus, species, date, gps_latitude, gps_longitude, gps_position_error, description, botanist, and newfile (the renamed/organised photo path from the photo renaming step)."
  },
  {
    "objectID": "inat_plan.html#outputs",
    "href": "inat_plan.html#outputs",
    "title": "iNat Upload Plan",
    "section": "",
    "text": "data/inat_batches/batch_01.csv, batch_02.csv, … — one CSV per 50 observations, each matching the iNat bulk uploader template exactly.\nThe photo files referenced by media_name_* columns are the renamed files produced by the photo reorganisation step in botanist_photo_processing.qmd (photo_all3$newfile)."
  },
  {
    "objectID": "inat_plan.html#column-mapping",
    "href": "inat_plan.html#column-mapping",
    "title": "iNat Upload Plan",
    "section": "",
    "text": "iNat field\nSource column / value\n\n\n\n\nid\nsequential integer within the full table (1, 2, 3, …)\n\n\ntaxon_name\npaste(genus, species)\n\n\ndate_obs\nformat(date, \"%d/%m/%Y\")\n\n\ntime_zone\n\"Africa/Johannesburg\" (fixed)\n\n\ndescription\ncollapsed description values for all photos of that species at that plot\n\n\ntag_list\n\"BioSCape\" (fixed)\n\n\nlatitude\nmean GPS latitude of all photos for that species × plot\n\n\nlongitude\nmean GPS longitude\n\n\nlocation\nBScpPID\n\n\npos_acc\nmax gps_position_error for that species × plot\n\n\ngeoprivacy\n\"open\" (fixed)\n\n\nfield_id1\n(empty)\n\n\nfield_value1\n(empty)\n\n\nmedia_name_1–media_name_10\nbasename(newfile) for each photo of that species at that plot (up to 10)"
  },
  {
    "objectID": "inat_plan.html#processing-steps",
    "href": "inat_plan.html#processing-steps",
    "title": "iNat Upload Plan",
    "section": "",
    "text": "Load data — read the versioned photo_all_file CSV from botanist_photo_processing.qmd.\nFilter — keep only photo_type == \"species\" rows with non-NA genus + species + BScpPID.\nGroup — group by BScpPID + taxon_name (one row per species per plot).\nCollapse photos — within each group, take up to 10 photos and spread their basenames into media_name_1 … media_name_10. Record n_photos for reference.\nAssign batch — add a batch column: ceiling(row_number() / 50).\nWrite output — for each unique batch value, write a CSV named data/inat_batches/batch_&lt;sprintf(\"%02d\", batch)&gt;.csv containing only the iNat template columns (no batch column in the output files)."
  },
  {
    "objectID": "inat_plan.html#bug-fixes-required-in-botanist_photo_processing.qmd-first",
    "href": "inat_plan.html#bug-fixes-required-in-botanist_photo_processing.qmd-first",
    "title": "iNat Upload Plan",
    "section": "",
    "text": "photo_type regex — the current pattern \"plot|S|N|W|E|start|rarefaction|end|stop\" matches single letters S/N/W/E anywhere inside a genus name (e.g. “Erica” → matches “E”). Fix: use \\\\b(plot|start|rarefaction|end|stop)\\\\b|^[SNWE]$ so single-letter cardinal directions only match when the entire genus field is that letter.\nst_drop_geometry() before write_csv — the sf geometry column is currently written into the CSV as WKT, which is not needed downstream. Drop it before writing."
  },
  {
    "objectID": "inat_plan.html#known-limitations-notes",
    "href": "inat_plan.html#known-limitations-notes",
    "title": "iNat Upload Plan",
    "section": "",
    "text": "The renamed photo files (newfile) are only generated when the photo renaming chunk in botanist_photo_processing.qmd has been run (eval=F currently — must be run manually). The iNat_prepare.qmd will validate that newfile is populated and warn if not.\niNat batch size = 50 observations per the user’s request.\nUp to 10 media files per observation (the template supports up to 25, but 10 is a practical limit given how many photos per species exist in the dataset)."
  },
  {
    "objectID": "Vegplot_Rarefaction.html",
    "href": "Vegplot_Rarefaction.html",
    "title": "BioSCape Vegetation Plot Photo Rarefaction",
    "section": "",
    "text": "This analysis processes vegetation rarefaction photography data to derive insights about species discovery over time and across different plots. The data includes vegetation photos taken at different plots, with each photo representing a unique species observation. The analysis includes the following steps:\n\nData Preparation: Load and preprocess vegetation photo data, including filtering out replicates and calculating time intervals between photos.\nRarefaction Curves: Visualize rarefaction curves for each plot, showing the number of unique species observed over time.\nSpecies Accumulation Curves: Fit a Michaelis-Menten model to each site’s species accumulation curve to estimate total species richness and sampling effort.\nComparison with Dominant Species Richness: Compare rarefaction estimates with dominant species richness from cover data.\nComparison with Total Vegetation Cover: Compare rarefaction estimates with total vegetation cover.\nUpload to Github Release: Publish preliminary version for review\n\n\n\nShow the code\n# Load necessary libraries\nlibrary(tidyverse)\nlibrary(vegan)\nlibrary(sf)\nlibrary(piggyback)\nlibrary(broom)\nlibrary(leaflet)\nlibrary(cowplot)\nlibrary(DT)\n# Downloading Data\nphoto_all_file &lt;- paste0(\"data/\", \"vegphoto_all_v20250228\", \".csv\")\n#pb_download(file = \"vegphoto_all_v20250228.csv\", dest = \"data\", tag = \"vegphoto_v20250228\")\ndata &lt;- read_csv(photo_all_file) %&gt;% \n  st_as_sf(crs = 4326, coords = c(\"gps_longitude\", \"gps_latitude\"))\n\n\n# Download spatial data\n#pb_download(file=\"BioScapeSpatialProducts2024_11_12.zip\",tag=\"v20241112\",dest = \"data\")\nunzip(\"data/BioScapeSpatialProducts2024_11_12.zip\",exdir = \"data/spatial\")\n\n\nplots=read_sf(\"data/spatial/BioSCapeVegCenters2024_11_12.shp\")\nhomogeneous=read_sf(\"data/spatial/BioSCapeVegPolys2024_11_12.shp\")\n\n# download vegplot cover data\npdatafile=\"bioscape_veg_plot_summary_v20241104.csv\"\n#pb_download(file=pdatafile,tag=\"v20241104\",dest = \"data\")\npdata=read_csv(file.path(\"data\",pdatafile))\n\nvdatafile=\"bioscape_veg_plot_species_v20241104.csv\"\n#pb_download(file=vdatafile,tag=\"v20241104\",dest = \"data\")\nvdata=read_csv(file.path(\"data\",vdatafile))"
  },
  {
    "objectID": "Vegplot_Rarefaction.html#summary",
    "href": "Vegplot_Rarefaction.html#summary",
    "title": "BioSCape Vegetation Plot Photo Rarefaction",
    "section": "",
    "text": "This analysis processes vegetation rarefaction photography data to derive insights about species discovery over time and across different plots. The data includes vegetation photos taken at different plots, with each photo representing a unique species observation. The analysis includes the following steps:\n\nData Preparation: Load and preprocess vegetation photo data, including filtering out replicates and calculating time intervals between photos.\nRarefaction Curves: Visualize rarefaction curves for each plot, showing the number of unique species observed over time.\nSpecies Accumulation Curves: Fit a Michaelis-Menten model to each site’s species accumulation curve to estimate total species richness and sampling effort.\nComparison with Dominant Species Richness: Compare rarefaction estimates with dominant species richness from cover data.\nComparison with Total Vegetation Cover: Compare rarefaction estimates with total vegetation cover.\nUpload to Github Release: Publish preliminary version for review\n\n\n\nShow the code\n# Load necessary libraries\nlibrary(tidyverse)\nlibrary(vegan)\nlibrary(sf)\nlibrary(piggyback)\nlibrary(broom)\nlibrary(leaflet)\nlibrary(cowplot)\nlibrary(DT)\n# Downloading Data\nphoto_all_file &lt;- paste0(\"data/\", \"vegphoto_all_v20250228\", \".csv\")\n#pb_download(file = \"vegphoto_all_v20250228.csv\", dest = \"data\", tag = \"vegphoto_v20250228\")\ndata &lt;- read_csv(photo_all_file) %&gt;% \n  st_as_sf(crs = 4326, coords = c(\"gps_longitude\", \"gps_latitude\"))\n\n\n# Download spatial data\n#pb_download(file=\"BioScapeSpatialProducts2024_11_12.zip\",tag=\"v20241112\",dest = \"data\")\nunzip(\"data/BioScapeSpatialProducts2024_11_12.zip\",exdir = \"data/spatial\")\n\n\nplots=read_sf(\"data/spatial/BioSCapeVegCenters2024_11_12.shp\")\nhomogeneous=read_sf(\"data/spatial/BioSCapeVegPolys2024_11_12.shp\")\n\n# download vegplot cover data\npdatafile=\"bioscape_veg_plot_summary_v20241104.csv\"\n#pb_download(file=pdatafile,tag=\"v20241104\",dest = \"data\")\npdata=read_csv(file.path(\"data\",pdatafile))\n\nvdatafile=\"bioscape_veg_plot_species_v20241104.csv\"\n#pb_download(file=vdatafile,tag=\"v20241104\",dest = \"data\")\nvdata=read_csv(file.path(\"data\",vdatafile))"
  },
  {
    "objectID": "Vegplot_Rarefaction.html#data-description",
    "href": "Vegplot_Rarefaction.html#data-description",
    "title": "BioSCape Vegetation Plot Photo Rarefaction",
    "section": "Data description",
    "text": "Data description\nThe rarefaction data are shared in two formats: photo-level data and plot-level data. The photo-level data include information about each photo taken, including the plot, species observed, and time of observation. The plot-level data summarize the rarefaction data for each plot, including the estimated species richness and sampling effort.\n\n\nShow the code\n# Assemble and organize photo-level data\nvegphoto_rarefied_photo &lt;- rdata %&gt;%\n  select(BScpPID, description, genus, species, species_replicate, datetime, time, \n         rarefaction_min, rarefaction_replicate, count, botanist) |&gt; \n  mutate(datetime=as.Date(datetime), # clean up classes for EML below\n         species_replicate=as.numeric(species_replicate))\n\n# Assemble and simplify plot data \nvegphoto_rarefied_plot &lt;- rdatas %&gt;%\n  left_join(select(results,BScpPID,Smax,B,Smax_Lower,Smax_Upper,B_Lower,B_Upper), by = \"BScpPID\") %&gt;%\n  select(BScpPID, datetime_start, total_cover, \n         rarefaction_n_5min, rarefaction_n_10min, rarefaction_n_max, rarefaction_replicates,\n         Smax, B, Smax_Lower,Smax_Upper,B_Lower,B_Upper)\n\n\n\nvegphoto_rarefied_photo - Column descriptions\n\nBScpPID - plot id aligned with spatial vegplot data\ndescription - original caption entered by the botanists in the field\ngenus - genus of the observed species (when available)\nspecies - species of the observed species (when available)\nspecies_replicate - flag indicating if the species is a replicate photo and should be filtered\ndatetime - date and time of the photo\ntime - time of day of the photo\nrarefaction_time - time since the first photo in the rarefaction sequence\nrarefaction_min - time since the first photo in the rarefaction sequence in minutes\nrarefaction_replicate - replicate number of the rarefaction sequence (some botanists recorded 4 separate rarefaction sequences, unfortunately)\ncount - cumulative count of unique species observed in the rarefaction sequence\nbotanist - name of the botanist who took the photo\n\n\n\nShow the code\nvegphoto_rarefied_photo |&gt;\n  ungroup() |&gt;\n  slice(1:20) |&gt;\n  datatable()\n\n\n\n\n\n\n\n\nvegphoto_rarefied_plot - Column descriptions\n\nBScpPID - plot id aligned with spatial vegplot data\ndatetime_start - date and time of the first photo in the rarefaction sequence\nrarefaction_n_5min - estimated species richness after 5 minutes of sampling\nrarefaction_n_10min - estimated species richness after 10 minutes of sampling\nrarefaction_n_max - maximum estimated species richness in the rarefaction sequence\nrarefaction_replicates - number of replicates in the rarefaction sequence (some botanists recorded 4 separate rarefaction sequences - filter to values equal to 1 to avoid these plots)\nSmax - estimated total species richness in the pool from the rarefaction. Note that some plots have very high (unrealisitic) values for this field.\nB - sampling effort needed to detect 50% of those species\n\n\n\nShow the code\nvegphoto_rarefied_plot |&gt;\n  slice(1:20) |&gt;\n  datatable()"
  },
  {
    "objectID": "Vegplot_Rarefaction.html#write-metadata",
    "href": "Vegplot_Rarefaction.html#write-metadata",
    "title": "BioSCape Vegetation Plot Photo Rarefaction",
    "section": "Write metadata",
    "text": "Write metadata\n\n\nShow the code\nlibrary(EML)\n\n# Define dataset metadata\ndataset &lt;- list(\n  title = \"BioSCape Vegetation Plot Photo Rarefaction\",\n  creator = list(\n    list(individualName = list(givenName = \"Adam M.\", surName = \"Wilson\")),\n    list(individualName = list(givenName = \"Jasper\", surName = \"Slingsby\"))\n  ),\n  pubDate = Sys.Date(),\n  abstract = \"This dataset includes vegetation photo rarefaction data collected across the BioSCape Vegetation Plots.  These photos were collected as a rapid way to estimate the total vascular plant species richness within each plot without doing a full vegetation survey.  This was done with a standard 10-minute search for new species and taking a photo whenever a new species was observed.  The analysis processes these data to infer species discovery over time, including species accumulation and rarefaction curves. Processing steps included data cleaning, metadata annotation, and the generation of species accumulation and rarefaction curves to quantify biodiversity patterns across the plots.\n\nPhoto-level data were structured to include timestamps, species identifications, and replicates, enabling time-based rarefaction analyses. Plot-level summaries were generated to estimate species richness, total vegetation cover, and rarefied species counts at defined time intervals (e.g., 5-minute and 10-minute rarefaction windows). Standardized metadata attributes, including temporal and geographic coverage, were incorporated using the Ecological Metadata Language (EML) framework to facilitate data sharing and reproducibility. The final metadata document provides a structured, machine-readable summary of the dataset, supporting further ecological analysis and integration with broader biodiversity monitoring efforts.\",\n  intellectualRights = \"This dataset is provided under the CC-BY license.\",\n  keywordSet = list(\n    list(keyword = c(\"vegetation\", \"rarefaction\", \"species richness\", \"ecology\", \"biodiversity\"))\n  ),\n  coverage = list(\n    geographicCoverage = list(\n      geographicDescription = \"BioSCape study region\",\n      boundingCoordinates = list(\n        westBoundingCoordinate = 18.01,\n        eastBoundingCoordinate = 23.45,\n        northBoundingCoordinate = -32.13,\n        southBoundingCoordinate = -34.81\n      )\n    ),\n    temporalCoverage = list(\n      rangeOfDates = list(\n        beginDate = list(calendarDate = \"2023-05-03\"),\n        endDate = list(calendarDate = \"2023-11-01\")\n      )\n    )\n  ),\n  contact = list(\n    individualName = list(givenName = \"Adam M.\", surName = \"Wilson\"),\n    electronicMailAddress = \"adamw@buffalo.edu\"\n  ),\n  methods = list(methodStep = list(description = \"Vegetation rarefaction photography data were processed to derive species accumulation and rarefaction curves. The workflow included data cleaning, rarefaction curve fitting, and model estimation. See comments in the code for more details.\"))\n)\n\n# Define entity metadata for data tables\ndataTable1 &lt;- list(\n  entityName = \"vegphoto_rarefied_photo.csv\",\n  entityDescription = \"Photo-level rarefaction dataset including information on individual photos, species observed, and observation timestamps. Most users will prefer to use the plot-level summaries in the other table.\",\n  physical = list(objectName = \"vegphoto_rarefied_photo.csv\"),\nattributeList &lt;- EML::set_attributes(\n  attributes = bind_rows(\n    data.frame(attributeName=\"BScpPID\", attributeDefinition=\"Plot ID\", unit= NA, \n               formatString = \"character\", measurementScale = \"nominal\",domain=\"textDomain\"),\n    data.frame(attributeName=\"description\",attributeDefinition= \"Photo caption as entered in the field\", unit= NA, \n               formatString = \"character\", measurementScale = \"nominal\",domain=\"textDomain\"),\n    data.frame(attributeName=\"genus\", attributeDefinition=\"Genus name\", unit= NA, \n               formatString = \"character\", measurementScale = \"nominal\",domain=\"textDomain\"),\n    data.frame(attributeName=\"species\", attributeDefinition=\"Species name\",unit= NA,\n               formatString = \"character\", measurementScale = \"nominal\",domain=\"textDomain\"),\n    data.frame(attributeName=\"species_replicate\", attributeDefinition=\"Flag for species replicate\",unit=  NA,\n               formatString = \"character\", measurementScale = \"nominal\",domain=\"textDomain\"),\n    data.frame(attributeName=\"datetime\",attributeDefinition= \"Timestamp\", unit= NA,\n               formatString = \"YYYY-MM-DD HH:MM:SS\", measurementScale = \"dateTime\",domain=\"dateTimeDomain\"), \n    data.frame(attributeName=\"time\",attributeDefinition= \"Time of day\",  unit=\"dateTime\", \n               formatString = \"character\", measurementScale = \"dateTime\",domain=\"dateTimeDomain\"), #\"HH:MM:SS\",\n    data.frame(attributeName=\"rarefaction_min\",attributeDefinition=\"Time since first photo in minutes\", unit=\"minutes\", \n               formatString = \"character\", measurementScale = \"nominal\",domain=\"textDomain\"),\n    data.frame(attributeName=\"rarefaction_replicate\",\n               attributeDefinition= \"Replicate number (some rarefaction sequences were recorded as multiple sub-series)\", unit= \"count\", \n               formatString = \"character\", measurementScale = \"nominal\",domain=\"textDomain\"),\n    data.frame(attributeName=\"count\",attributeDefinition= \"Cumulative species count\", unit= \"count\", \n               formatString = \"character\", measurementScale = \"nominal\",domain=\"textDomain\"),\n    data.frame(attributeName=\"botanist\",attributeDefinition= \"Botanist's name\",unit= NA, \n               formatString = \"character\", measurementScale = \"nominal\",domain=\"textDomain\")\n  )))\n\nlibrary(dplyr)\n\ndataTable2 &lt;- list(\n  entityName = \"vegphoto_rarefied_plot.csv\",\n  entityDescription = \"Plot-level summary of species accumulation and rarefaction estimates.\",\n  physical = list(objectName = \"vegphoto_rarefied_plot.csv\"),\n  attributeList = EML::set_attributes(bind_rows(\n    data.frame(\n      attributeName = \"BScpPID\",\n      attributeDefinition = \"Plot ID\",\n      formatString = \"character\",\n      measurementScale = \"nominal\",\n      domain=\"textDomain\"\n    ),\n    data.frame(\n      attributeName = \"datetime_start\",\n      attributeDefinition = \"Start timestamp\",\n      formatString = \"YYYY-MM-DD HH:MM:SS\",\n      measurementScale = \"dateTime\",\n      domain=\"dateTimeDomain\"\n    ),\n    data.frame(\n      attributeName = \"total_cover\",\n      attributeDefinition = \"Total vegetation cover from the bioscape vegetation dataset\",\n      formatString = \"numeric\",\n      measurementScale = \"nominal\",\n      domain=\"textDomain\"\n    ),\n    data.frame(\n      attributeName = \"rarefaction_n_5min\",\n      attributeDefinition = \"Species richness after 5 minutes\",\n      formatString = \"integer\",\n      measurementScale = \"nominal\",\n      domain=\"textDomain\"\n    ),\n    data.frame(\n      attributeName = \"rarefaction_n_10min\",\n      attributeDefinition = \"Species richness after 10 minutes\",\n      formatString = \"integer\",\n      measurementScale = \"nominal\",\n      domain=\"textDomain\"\n    ),\n    data.frame(\n      attributeName = \"rarefaction_n_max\",\n      attributeDefinition = \"Maximum species richness observed (some surveys went over 10 mintues)\",\n      formatString = \"integer\",\n      measurementScale = \"nominal\",\n      domain=\"textDomain\"\n    ),\n    data.frame(\n      attributeName = \"rarefaction_replicates\",\n      attributeDefinition = \"Number of replicates of the rarefaction procedure that were done accidentally. Filter out plots with rarefaction_replicates&gt;1 to remove these plots.\",\n      formatString = \"integer\",\n      measurementScale = \"nominal\",\n      domain=\"textDomain\"\n    ),\n    data.frame(\n      attributeName = \"Smax\",\n      attributeDefinition = \"Estimated total species richness\",\n      formatString = \"numeric\",\n      measurementScale = \"nominal\",\n      domain=\"textDomain\"\n    ),\n    data.frame(\n      attributeName = \"B\",\n      attributeDefinition = \"Sampling effort to detect 50% of species\",\n      formatString = \"numeric\",\n      measurementScale = \"nominal\",\n      domain=\"textDomain\"\n    ),\n        data.frame(\n      attributeName = \"Smax_Upper\",\n      attributeDefinition = \"Estimated total species richness, 95% upper confidence interval\",\n      formatString = \"numeric\",\n      measurementScale = \"nominal\",\n      domain=\"textDomain\"\n    ),\n    data.frame(\n      attributeName = \"Smax_Lower\",\n      attributeDefinition = \"Estimated total species richness, 95% lower confidence interval\",\n      formatString = \"numeric\",\n      measurementScale = \"nominal\",\n      domain=\"textDomain\"\n    ),\n    data.frame(\n      attributeName = \"B_Upper\",\n      attributeDefinition = \"Sampling effort (time) to detect 50% of species, 95% upper confidence interval\",\n      formatString = \"numeric\",\n      measurementScale = \"nominal\",\n      domain=\"textDomain\"\n    ),\n    data.frame(\n      attributeName = \"B_Lower\",\n      attributeDefinition = \"Sampling effort (time) to detect 50% of species, 95% lower confidence interval\",\n      formatString = \"numeric\",\n      measurementScale = \"nominal\",\n      domain=\"textDomain\"\n    )\n  ))\n)\n# Create EML document\neml &lt;- list(dataset = dataset, dataTable = list(dataTable1, dataTable2))\nvegphoto_rarefied_metadata_file = \"data/vegphoto_metadata.xml\"\nwrite_eml(eml, vegphoto_rarefied_metadata_file)\n\n\n\n\nShow the code\ntagdate=paste0(\"v\",format(today(),\"%Y%m%d\"))\ntag=paste0(\"vegphoto_\",tagdate)\n\n\nvegphoto_rarefied_plot_file=paste0(\"data/vegphoto_rarefied_plot_\",tagdate,\".csv\")\nwrite_csv(vegphoto_rarefied_plot,vegphoto_rarefied_plot_file)\n\nvegphoto_rarefied_photo_file=paste0(\"data/vegphoto_rarefied_photo_\",tagdate,\".csv\")\n\nvegphoto_rarefied_photo |&gt;\n  st_set_geometry(NULL)|&gt;\nwrite_csv(vegphoto_rarefied_photo_file)\n\n\n# tag = \"vegphoto_v20241126\"\nif(F){\n  print(tag)\n# upload to github release is manual - run the pb_upload command if desired\n  pb_new_release(\"BioSCape-io/BioSCape-terrestrial\",tag=tag)\n  pb_upload(vegphoto_rarefied_plot_file,tag=tag)\n  pb_upload(vegphoto_rarefied_photo_file,tag=tag)\n  pb_upload(vegphoto_rarefied_metadata_file, tag=tag)\n}"
  },
  {
    "objectID": "Vegplot_Rarefaction.html#rarefaction-estimates-compared-to-dominant-species-richness",
    "href": "Vegplot_Rarefaction.html#rarefaction-estimates-compared-to-dominant-species-richness",
    "title": "BioSCape Vegetation Plot Photo Rarefaction",
    "section": "Rarefaction estimates compared to Dominant Species Richness",
    "text": "Rarefaction estimates compared to Dominant Species Richness\n\n\nShow the code\n#vegphoto_rarefied_plot |&gt; \nrdatas |&gt;\nggplot(aes(x = n_species, y = rarefaction_n_10min)) +\n  geom_point()+#aes(color=rarefaction_replicates&gt;1))+\n  geom_smooth(method = \"lm\") +\n#  geom_text(aes(label = BScpPID), nudge_x = 0.5, nudge_y = 0.5) +\n  geom_abline(intercept = 0, slope = 1, linetype = \"dashed\") +\n  labs(x = \"# Species in Cover Data\", y = \"# Photos in Rarefaction Data\")+\n  coord_cartesian(ylim=c(0,75))"
  },
  {
    "objectID": "Vegplot_Rarefaction.html#rarefaction-estimates-compared-to-total-vegetation-cover",
    "href": "Vegplot_Rarefaction.html#rarefaction-estimates-compared-to-total-vegetation-cover",
    "title": "BioSCape Vegetation Plot Photo Rarefaction",
    "section": "Rarefaction estimates compared to total vegetation cover",
    "text": "Rarefaction estimates compared to total vegetation cover\n\n\nShow the code\nrdatas |&gt;\nggplot(aes(x = total_cover, y = rarefaction_n_10min)) +\n  geom_point() +\n  geom_smooth(method = \"lm\") +\n  geom_text(aes(label = BScpPID), nudge_x = 0.5, nudge_y = 0.5) +\n  labs(x = \"Total Vegetation Cover (%)\", y = \"# Photos in Rarefaction Data\")"
  }
]